# Python Application

This directory will contain the small backend API, its dependency manifest, and local validation notes.

Planned endpoints:

- `/` — basic application response
- `/health` — health-check response

Implementation and test evidence will be added during the application deployment phase.
