# Reverse Proxy Connectivity Incident

## Scenario

A guided incident will test the request path between the client, Azure network controls, the frontend NGINX listener, and the backend private endpoint.

## Incident record

The observed symptom, layer-by-layer tests, root cause, correction, and end-to-end validation will be added after troubleshooting is complete.
