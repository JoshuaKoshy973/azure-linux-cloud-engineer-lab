# NGINX Configuration

This directory will contain the reviewed NGINX reverse-proxy configuration used by the frontend Linux VM.

The final documentation will record the listener, backend private address and port, configuration validation, reload procedure, and end-to-end test.
