# systemd Configuration

This directory will contain the application service unit used on the backend Linux VM.

The completed service definition will document its restricted user, working directory, start command, environment handling, restart behavior, boot enablement, and log-validation procedure.
